# `disk-clean` used to be listed under `exclude:` here. It is no longer a checker
# at all — it moved to the `quality-gate clean` subcommand because it mutates the
# tree — so the exclusion referred to nothing and was dropped rather than renamed.
enabledCheckers:
  - all
  - logging

consistency:
  corpusPath: /Users/jpurnell/Dropbox/Computer/Development/Swift/Tools/org-judgement-corpus
  projectID: SwiftXLSX
  consistencyThreshold: 0.7
  defaultRiskTier: 2

# v2 layout: the master plan is project-owned, not framework content.
status:
  guidelinesPath: "."
  masterPlanPath: project/master_plan.md
